apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-service-http-ingress
spec:
  rules:
    - host: http.auth.money-tracker.dev.vasapolrittideah.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: auth-service
                port:
                  name: http
