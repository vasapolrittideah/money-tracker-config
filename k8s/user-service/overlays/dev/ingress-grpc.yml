apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: user-service-grpc-ingress
spec:
  rules:
    - host: grpc.user.money-tracker.dev.vasapolrittideah.com
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: user-service
                port:
                  name: grpc
